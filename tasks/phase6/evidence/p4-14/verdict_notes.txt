3D Journey (feature 14) — plume-cloud portion, checked today against the real
running frontend (npm run dev, port 5173) with a fresh Playwright run (6/6 passed,
see playwright_run.txt), not re-quoting the Phase 4/5 write-ups.

WHERE PRESENT, REAL:
The plume layer's extruded shape (journey3d-transport.png, journey3d-accumulation.png)
is driven by frontend/public/fixtures/journey3d.json, whose own embedded fields
(fixture_real_data_fields.json) show is_stub: false, model_version:
custom_2d-calibrated-AQ-2016-10-28, plume_source: REAL_PARTICLE_ENGINE — matching,
field for field, today's fresh core-C live API check
(tasks/phase6/evidence/core-c/plume_simulate.json) for the same event/outlet
(AQ-2016-10-28 / AQ-O02). This is not a coincidence to take on faith: the fixture is
generated by scripts/frontend_journey.py calling the exact same /plume/simulate +
/exposure/calculate routes core-C just re-confirmed live. The on-screen caption also
carries the real current-masking caveat verbatim (current_masking_caveat field above),
not a softened or dropped version of it.

BLOCKED-NOT-BUILT (explicitly, not silently faked):
This is a real 2D KDE contour (kernel_density_contours' peak-normalized density
levels) extruded into a 3D shape — real geometry, real probability data, honestly
labelled as such in the UI copy ("the plume's height encodes its real contour
probability level, not a physical thickness"). It is NOT a true volumetric cloud of
individually-tracked, depth-resolved particles. That specific capability remains
unbuilt: SimulationResult.lons/.lats (per-particle positions) exist in memory inside
simulate() but nothing persists or serves raw per-particle positions via the API, and
the particle engine is 2D (no literal per-particle z-coordinate) — both gaps already
named in docs/HANDOFF_abd_2026-08-06.md §5 ("What a genuine volumetric 3D plume cloud
would still need"), re-confirmed still true by grepping backend/src/api/main.py today:
no route serves raw particle positions.

VERDICT BASIS: PASS for "driven by real particle-engine output where present" (proven
today, live, field-for-field); the true-volumetric-cloud capability is correctly
BLOCKED-NOT-BUILT and the UI does not claim otherwise anywhere checked this pass.
