# Worker image. Everything the API has, plus the geospatial stack the
# pipeline and the simulations need. Bigger, and rebuilt far less often.
#
# Pinned to versions that ship cp312 manylinux wheels. This matters: an
# earlier attempt pinned rasterio==1.4.3, which has no wheel here, so pip fell
# back to building from source and demanded gdal-config. Resolve with
# --only-binary=:all: so a source build fails loudly at resolve time rather
# than surfacing as a missing system library halfway through a docker build:
#   docker run --rm python:3.12-slim sh -c \
#     "pip install -q --only-binary=:all: <pkgs> && pip freeze"
-r requirements-api.txt
geopandas==1.1.4
rasterio==1.5.0
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0
whitebox==2.3.6
requests==2.34.2
